import java.math.BigInteger;
public class Q2 {
    /*
    public static void main(String[] args) {
        //2. 1번 문제에서 32번째 항이 21억 정도가 나올 것이다.
        //BigInteger를 통해서 50번째 항을 구해보자!

        //Q. 앞선 코드작성을 완성하지 못해 32번쨰 항을 구하지 못했으나
        // 작성했던 것을 토대로 BigInteger 변환해봤습니다.


        final int START = 1;

        BigInteger[] sequence; // (등비) 수열을 구하여라 // 큰값을 위해 Biginteger

        Scanner sc = new Scanner(System.in); // 원하는 값을 뽑아내도록 스캐너

        System.out.print("등비 수열의 원하는 값을 뽑아내보자 ");
        int end = sc.nextInt();

        sequence = new BigInteger[end];

        sequence[0] = new BigInteger("1"); // 초항 작성
        sequence[1] = new BigInteger("2");

        // for문을 이용해서 등비수열을 구하는 방법
        for (int i = START; i < end; i++) { // 질문Q.등비수열의 식을 어떻게 표현할까요?
            sequence[i] = sequence[].add sequence[ ];
            System.out.println("arr[" + i + "] = " + sequence[i]);
        }
    }
}


 */