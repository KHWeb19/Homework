public class Explain {
    // qSkill
    // wSkill
    // attack
    // -> 여기저기에서 사용하니 인터페이스로 구현.

    // 모험가 -> 전사 -> 기사
    // 모험가 -> 법사 -> 위자드

    // 6. 기본 스탯 (phy atk, mag atk, hp, mp, phy def, mag def,
    //              str(완력), con(vit:체력), dex(재주), agi(민첩), iq(지능), men(정신))

    // 그럼 전사랑 법사랑 둘다 시작점인 모험가에서 기본스탯을 주자.
    // 그리고 모험가를 부모클래스로 만들면 되지않을까..?
    // 그리고 2차 전직을 했으면 1차 전직을 상속하면 그대로 그대로 받을 수 있을것이다.


}
