/*public class Variance {
    final int SUBJECT_NUM = 3;
    Score score = new Score();
    double avg ,sum;*/

    /*public void initvar() {

        //분산 각 점수에 평균뺀 값에서 더한 제곱
        score.initScore();

        avg = score.sum / SUBJECT_NUM;

        //mathScore , englishScore, koreanScore 에 static 붙이니깐 오류 없음

        //★★★★Q. mathScore,englishScore,koreanScore은 입력 받는 값들인데
        // 이 값들은  Score클래스를 score으로 객체화해서 값을 가져오려해도 static으로 바꾸지 않으면 계속 오류가생김
        //
        double sum1 = (score.mathScore - avg);
        double sum2 = (score.englishScore - avg);
        double sum3 = (score.koreanScore - avg);

          sum = Math.pow(sum1, 2) + Math.pow(sum2, 2) + Math.pow(sum3, 2);

        //math.pow 더블형이라 float 붙임
        // variance = (float) Math.pow(sum ,2) / SUBJECT_NUM;
    }
        public void var(){
        System.out.println("분산은 " + sum/ SUBJECT_NUM);




    }
}*/
