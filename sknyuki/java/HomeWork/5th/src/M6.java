import java.math.BigInteger;

public class M6 {
    public static void main(String[] args) {
        //45678911234라는 숫자를 BigInteger에 배치한다.
        //각 자리수에 맞는 수 배치

        //45678911234를 BigInteger로 변환
        // 해당 숫자를 string으로 변환(각 숫자들을 "문자"로서 인식시키기위해)
        //변환 시킨 각 숫자를 배열에 대입시킨다.
        //
        //for문을 돌려서 arr[i]=로 루프를 돌린다?
        /*
        BigInteger num = new BigInteger("45678911234");

        String sNum = String.valueOf(num);//변환

        System.out.println(sNum.getClass());//타입확인

        String[] stringArray = new String[Integer.parseInt(sNum)];

        for (int i = 0; i <= sNum.length(); i++) {
            System.out.printf("arr[%d]=%d\n", i, sNum[sNum.length() - i]);

         */
            //for문 이하에서 항을 순차적으로 빼내는 법을 모르겠어요
            //여기서 배열을 어떻게 살려서 써야할까요..살려주세요
            //내머리가 나빠서♬-ss501
        }
    }
