클래스를 바로 배열화?
클래스 변수명=new 클래스();



생성자란?
생성자+this.는 필수가 되는가?

변수명만 다르게 주어서 다른 함수취급
->같은 공식 이면 입력값만 다르게해서 재활용가능

 Dice[] diceArr;
=>변수 

diceArr = new Dice[2];
=>배열화

diceArr[i] = new Dice();
=>객체화 

객체2개로 이루어져 있는 배열  

클래스 DIce와 메소드Dice의 구분이 어떻게 되는지


10000번이 아닌 한번만 출력이 되는 이유 



array는 객체화 시킬시 this.array= array.clone();을 붙어줘야 판독이 쉬워짐

생성자 new class();
객체화를 시키기위한 프로세스 

객체 메모리에 정보가 올라간 상태의 녀석

객체화 메모리에 정보를 올리는 작업

public Class(int age, int high){
this.age=age;
this.high=high;}
=>생성자
객체를 생성함과 동시에 자동으로 수행되어 맴버변수들의 값을 특정한 값으로 초기화 > 생성자


Class class=new Class(15,164);
 class: 클래스 Class가 객체화된 객체 
new Class ->객체화(인스턴스화)

 
호출
class.메소드
->추상화
예시)'짜장면' 주세요
춘장에 면을 버무린 과정을 통해 만들어진 음식을 '짜장면'이라고 추사화시켜서 주문함 



어제 객체화 설명하실때 사용하신 "짜장면"의 추상화와 같은 맥략인거죠?
더 큰 범주의 얘기가 되는건가요? 


상속=>다른 클래스(부모)에서 정보를 그대로 끌어오거나 추가 확장이 가능한 용도 
다이스 문제
이미 각 클래스별로 다루는 정보를 나뉘어져 있으며, 필요에따라 각 클래스의 메소드를 끌어오는 형식으로 이미 문제들이 해결되어있음 
여기서 무엇을 어떻게 상속화를 시켜서 문제를 재해석을 하겠다는건지가 갈피가 안 잡힘
나누는 의미가 있을까?

 DiceGame에서 GameManager객체화 시킬때 왜  new GameManager(id, DICE_NUM);에서 플레이어 수입력난에 id를 넣은 이유를 설명해주실수있을까요?


은행 업무로 따지면 각 은행별로 대출한도에 관한 코드를 짤때 금융결제원이 가이드라인을 interface로 던져준다고 하더라고요.

"세부적인 코드는 오버라이딩해서 마음대로 짜도 되지만 상수(대출한도)는 이거만써라, 메소드는 이걸 참조해라"

이를 안지키고 자신만의 메소드로 구현하면 금융결제원에서 제공해주는 그 어떠한 서비스도 호환이 안되어서 사용을 못한다고 하더라고요.
제가 이해하고 있는 방식도 맞나요?

->스킬 interface만들기 Q W추상 메소드 만들기 스킬

->모험가 클래스에서 초기 스텟과 모험가 스킬구현

->오버라이딩불러와서 초보자 스킬 반영 (능력치를 반영해서 스킬 만들기)

-> 마법사클래스와 전사클래스를 만들고 모험가클래스로부터 상속후, 스텟 추가구현(모험가 클래스에setter사용후 끌고오기)

->각각 위자드와 기사에 상속후 (특전)스킬과 스텟 추가구현

-> 1,2차 직업군에 오버라이드로 Q,W스킬 불러오기(getter사용후 플러스된 능력치를 반영해서 새로운 스킬 만들기)

->Q,W스킬구사 

->롤 플레잉 클래스 만들기 일단 위자드만 

->매크로를 돌릴거기에 매크로클래스 만들고 롤플레잉에 상속시킴

->매크로 클래스에 domacroset메서드 만들기 

->레벨엎 메서드 만들어서 호출하기

->레벨엎메서드는 어드벤쳐 클래스에서 LV의 초기화가 모험자에서 이루어졌기에

->레벨엎 할떄마다 스텟증가 메서드도 모험가 클래스에서 만듦



질문:1
차와 2차때 늘어난 능력치값을 스킬에 어떻게 다르게 방영하는지 
1차에서 증가된 능력치를 2차로 가져올수있는건지?
setter와getter가 0차에 있으니 0차스킬에서 증진값이 반영되는건 아닌건지?

질문2:
상속이 여기서 어떻게 각 코드에 영향을 주었는지 설명해주실수있을까요?
상속이 되었으면 부모의 데이터를 가지고있는건데 setter gsetter사용 해야하나요?

질문3:
매크로 매서드에서 object 위자드 객체를 넘긴다의 의미는?
object obj의 역할은?

매크로 메서드에
저trun옆 obj는 무엇을 의미하는 코드죠? 
기사와 위자드의 클래스를 호출하는건가요? 뒤에 붙은 "obj;"는 무슨 역할을 하는거죠?

질문 4 
모험가 클래스에서의 레벨엎 루프 
gettingExps-=reqExp;는 뭐죠? 매루프마다 얻는 경험치에서 요구되는 경험치를 빼라는 명령인가요?
tmp에서 이미 수행중인 명령코드가 아닌가요?
누적? 무엇을 누적?

질문5
