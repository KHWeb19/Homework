public class SquareArea implements Runnable{
    int width;
    int height;
    float bias;

    public SquareArea(int width, float bias){
        this.width = width;
        this.bias = bias;
    }

    // 개수를 어떻게 구하며
    // 구해진 개수를 어떤식으로 저장해야할까...
    @Override
    public void run() {
       // for (int i = 0; )
    }
}
